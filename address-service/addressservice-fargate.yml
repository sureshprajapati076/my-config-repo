env:
  name: This is from github repo with fargate profile
eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://internal-eureka-alb-2115340972.us-east-1.elb.amazonaws.com/eureka/
  instance:
    leaseExpirationDurationInSeconds: 1
    leaseRenewalIntervalInSeconds: 28
    prefer-ip-address: true
