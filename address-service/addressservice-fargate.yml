env:
  name: This is from github repo with fargate profile for address service
eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: ${EUREKA_DEFAULT_ZONE : http://localhost:8761/eureka/}
  instance:
    leaseExpirationDurationInSeconds: 1
    leaseRenewalIntervalInSeconds: 28
    prefer-ip-address: true
